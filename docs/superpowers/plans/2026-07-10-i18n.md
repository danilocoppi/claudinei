# i18n (en/es/pt-BR) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** UI inteira em en (default), es e pt-BR via react-i18next, com seletor de bandeirinhas à direita do logo, persistido em localStorage.

**Architecture:** `web/src/i18n/` concentra init + dicionários tipados (`es`/`pt-BR` são `: typeof en` — faltar chave é erro de compilação; `i18next.d.ts` tipa as chaves no `t()`). O setup de teste fixa `pt-BR` para preservar as ~120 asserções existentes. A migração dos componentes é mecânica: literal → `t('chave')` conforme o dicionário.

**Tech Stack:** `i18next` + `react-i18next` (SEM languagedetector — default é sempre `en`). React 18, vitest.

## Global Constraints

- Default **`en`** na primeira visita (nunca detectar navegador). Persistência: chave `claudinei.locale` no localStorage. `fallbackLng: 'en'`.
- `web/src/test/setup.ts` fixa `pt-BR` — as asserções pt existentes não são reescritas.
- `<html lang>` acompanha o idioma. Título do documento "Claudinei" não traduz (marca).
- Mensagens de erro vindas do BACKEND são exibidas como chegam (fora do escopo).
- Menu do seletor via **createPortal** (a .sidebar tem backdrop-filter — lição registrada).
- Ao migrar um componente: TODO literal visível ao usuário vira `t()`. Se encontrar um literal que não está na tabela, criar a chave na convenção da área nas 3 línguas e anotar no report.

---

### Task 1: Infra i18n + dicionários completos + LanguageSwitcher + Sidebar migrada

**Files:**
- Create: `web/src/i18n/en.ts`, `web/src/i18n/es.ts`, `web/src/i18n/pt-BR.ts`, `web/src/i18n/index.ts`, `web/src/i18n/i18next.d.ts`
- Create: `web/src/components/LanguageSwitcher.tsx`
- Modify: `web/src/main.tsx` (import './i18n'), `web/src/test/setup.ts` (fixa pt-BR), `web/src/components/Sidebar.tsx` (migra strings + coloca o switcher na linha do logo), `web/src/styles.css` (classes do switcher), `web/src/test/sidebar.test.tsx` (troca import de STATUS_LABEL por literal pt)
- Test: `web/src/test/i18n.test.tsx` (create)

**Interfaces:**
- Produces: `useTranslation()` funcionando em toda a app; dicionários completos (Tasks 2-3 só consomem chaves já definidas aqui); `LanguageSwitcher` (sem props).
- `types.ts` STATUS_LABEL **permanece** (ChatView/ProjectCard ainda usam) — a Sidebar passa a usar `t('status.'+s.status)`; STATUS_LABEL sai na Task 3.

- [ ] **Step 1: Instalar dependências**

Run: `npm install i18next@^25.0.0 react-i18next@^15.0.0 -w web`

- [ ] **Step 2: Dicionário base `web/src/i18n/en.ts` (fonte da tipagem — COMPLETO)**

```ts
export const en = {
  common: {
    cancel: 'Cancel', save: 'Save', create: 'Create', send: 'Send',
    delete: 'Delete', edit: 'Edit', preview: 'Preview',
  },
  lang: { label: 'Language' },
  sidebar: {
    terminals: 'Terminals', addTerminal: '+ Terminal', noSession: 'no session',
    empty: 'No terminals yet — create the first one with “+ Terminal”.',
    interaction: 'Terminal interaction', mural: 'Board', tasks: 'Tasks',
    overview: 'Overview',
    revive: 'Revive', startSession: 'Start session', editTerminal: 'Edit', deleteTerminal: 'Delete',
  },
  status: {
    starting: 'ready — send a message', idle: 'idle', working: 'working',
    needs_attention: 'waiting for you', stopped: 'finished', dead: 'dead',
    in_terminal: 'in terminal',
  },
  dashboard: {
    projects: 'Projects',
    empty: 'No projects yet. Create the first one!',
  },
  projectCard: { deleteTitle: 'Delete project', startSession: 'Start session', revive: 'Revive' },
  confirm: {
    deleteTitle: 'Delete {{name}}?',
    deleteMsg: 'This removes the terminal from Claudinei’s list. It does not delete files on disk nor the conversation history.',
  },
  modal: {
    newProject: 'New project', editTerminal: 'Edit terminal',
    namePlaceholder: 'Project name', choosePath: 'Choose folder…', previewTag: 'preview',
  },
  folder: { title: 'Choose folder', empty: 'No subfolders.', select: 'Select this folder' },
  session: {
    title: 'New session',
    model: 'Model', modelDesc: 'Default uses your Claude configuration; pick a specific model if you prefer.',
    modelDefault: 'Default', modelFable: 'Fable (most capable)', modelOpus: 'Opus',
    modelSonnet: 'Sonnet', modelHaiku: 'Haiku (fastest)',
    continueTitle: 'Continue conversation', continueDesc: 'Resumes the latest conversation of this folder, if any.',
    skipTitle: 'Skip permissions', skipDesc: 'Claude runs commands without asking for your confirmation at each step.',
    permWarning: 'With this off, actions that need permission are auto-denied in the web chat. To approve interactively, use “Open in terminal”.',
    start: 'Start session',
  },
  chat: {
    select: 'Select a session.', openInTerminal: 'Open in terminal',
    inTerminalNotice: 'This session is open in the terminal. Close it there to resume on the web.',
    placeholder: 'Message for Claude Code… (drag or paste files)',
    placeholderWorking: 'session working…', placeholderUploading: 'uploading attachment…',
    turnDone: 'turn finished', turnError: 'turn ended with an error',
    thinking: 'Thinking', forward: 'Forward', forwardedTo: 'forwarded to {{name}}',
    noAgents: 'No other active agents', subagent: 'subagent',
  },
  toolcall: { result: 'Result:' },
  terminal: {
    close: 'End terminal', reconnecting: 'disconnected — reconnecting…',
    disconnected: 'disconnected', reconnect: 'Reconnect',
    openError: '[error opening terminal: {{error}}]',
  },
  mural: { title: 'Board', empty: 'The board is empty. Agents publish notices here.' },
  tasks: {
    title: 'Tasks',
    empty: 'No tasks dispatched yet. An agent can delegate with the despachar_tarefa tool.',
    inProgress: 'in progress', done: 'done', failed: 'failed', operator: 'operator',
  },
  notify: { needsAttention: 'finished and awaits you', died: 'session died' },
}
export default en
```

- [ ] **Step 3: `web/src/i18n/pt-BR.ts` (COMPLETO, tipado)**

```ts
import type en from './en'

export const ptBR: typeof en = {
  common: {
    cancel: 'Cancelar', save: 'Salvar', create: 'Criar', send: 'Enviar',
    delete: 'Excluir', edit: 'Editar', preview: 'Prévia',
  },
  lang: { label: 'Idioma' },
  sidebar: {
    terminals: 'Terminais', addTerminal: '+ Terminal', noSession: 'sem sessão',
    empty: 'Nenhum terminal ainda — crie o primeiro no “+ Terminal”.',
    interaction: 'Interação entre terminais', mural: 'Mural', tasks: 'Tarefas',
    overview: 'Visão geral',
    revive: 'Reviver', startSession: 'Iniciar sessão', editTerminal: 'Editar', deleteTerminal: 'Excluir',
  },
  status: {
    starting: 'pronta — envie uma mensagem', idle: 'ociosa', working: 'trabalhando',
    needs_attention: 'aguardando você', stopped: 'finalizada', dead: 'morta',
    in_terminal: 'no terminal',
  },
  dashboard: {
    projects: 'Projetos',
    empty: 'Nenhum projeto ainda. Crie o primeiro!',
  },
  projectCard: { deleteTitle: 'Excluir projeto', startSession: 'Iniciar sessão', revive: 'Reviver' },
  confirm: {
    deleteTitle: 'Excluir {{name}}?',
    deleteMsg: 'Isso remove o terminal da lista do Claudinei. Não apaga os arquivos no disco nem o histórico de conversas.',
  },
  modal: {
    newProject: 'Novo projeto', editTerminal: 'Editar terminal',
    namePlaceholder: 'Nome do projeto', choosePath: 'Escolher pasta…', previewTag: 'pré-visualização',
  },
  folder: { title: 'Escolher pasta', empty: 'Nenhuma subpasta.', select: 'Selecionar esta pasta' },
  session: {
    title: 'Nova sessão',
    model: 'Modelo', modelDesc: 'Padrão usa a configuração do Claude; escolha um modelo específico se preferir.',
    modelDefault: 'Padrão', modelFable: 'Fable (mais capaz)', modelOpus: 'Opus',
    modelSonnet: 'Sonnet', modelHaiku: 'Haiku (mais rápido)',
    continueTitle: 'Continuar conversa', continueDesc: 'Retoma a última conversa desta pasta, se houver uma.',
    skipTitle: 'Pular permissões', skipDesc: 'O Claude executa comandos sem pedir sua confirmação a cada passo.',
    permWarning: 'Com isto desligado, ações que exigem permissão são negadas automaticamente no chat web. Para aprovar interativamente, use o “Abrir no terminal”.',
    start: 'Iniciar sessão',
  },
  chat: {
    select: 'Selecione uma sessão.', openInTerminal: 'Abrir no terminal',
    inTerminalNotice: 'Esta sessão está aberta no terminal. Feche a janela do terminal para poder retomar na web.',
    placeholder: 'Mensagem para o Claude Code… (arraste ou cole arquivos)',
    placeholderWorking: 'sessão trabalhando…', placeholderUploading: 'enviando anexo…',
    turnDone: 'turno concluído', turnError: 'turno terminou com erro',
    thinking: 'Pensamento', forward: 'Encaminhar', forwardedTo: 'encaminhado para {{name}}',
    noAgents: 'Nenhum outro agente ativo', subagent: 'subagente',
  },
  toolcall: { result: 'Resultado:' },
  terminal: {
    close: 'Encerrar terminal', reconnecting: 'desconectado — reconectando…',
    disconnected: 'desconectado', reconnect: 'Reconectar',
    openError: '[erro ao abrir terminal: {{error}}]',
  },
  mural: { title: 'Mural', empty: 'O mural está vazio. Os agentes publicam avisos aqui.' },
  tasks: {
    title: 'Tarefas',
    empty: 'Nenhuma tarefa despachada ainda. Um agente pode delegar tarefas com a ferramenta despachar_tarefa.',
    inProgress: 'em andamento', done: 'concluída', failed: 'falhou', operator: 'operador',
  },
  notify: { needsAttention: 'terminou e aguarda você', died: 'sessão morreu' },
}
export default ptBR
```

- [ ] **Step 4: `web/src/i18n/es.ts` (COMPLETO, tipado)**

```ts
import type en from './en'

export const es: typeof en = {
  common: {
    cancel: 'Cancelar', save: 'Guardar', create: 'Crear', send: 'Enviar',
    delete: 'Eliminar', edit: 'Editar', preview: 'Vista previa',
  },
  lang: { label: 'Idioma' },
  sidebar: {
    terminals: 'Terminales', addTerminal: '+ Terminal', noSession: 'sin sesión',
    empty: 'Aún no hay terminales — crea el primero con “+ Terminal”.',
    interaction: 'Interacción entre terminales', mural: 'Tablón', tasks: 'Tareas',
    overview: 'Vista general',
    revive: 'Reanudar', startSession: 'Iniciar sesión', editTerminal: 'Editar', deleteTerminal: 'Eliminar',
  },
  status: {
    starting: 'lista — envía un mensaje', idle: 'inactiva', working: 'trabajando',
    needs_attention: 'esperándote', stopped: 'finalizada', dead: 'muerta',
    in_terminal: 'en la terminal',
  },
  dashboard: {
    projects: 'Proyectos',
    empty: 'Aún no hay proyectos. ¡Crea el primero!',
  },
  projectCard: { deleteTitle: 'Eliminar proyecto', startSession: 'Iniciar sesión', revive: 'Reanudar' },
  confirm: {
    deleteTitle: '¿Eliminar {{name}}?',
    deleteMsg: 'Esto quita el terminal de la lista de Claudinei. No borra los archivos del disco ni el historial de conversaciones.',
  },
  modal: {
    newProject: 'Nuevo proyecto', editTerminal: 'Editar terminal',
    namePlaceholder: 'Nombre del proyecto', choosePath: 'Elegir carpeta…', previewTag: 'vista previa',
  },
  folder: { title: 'Elegir carpeta', empty: 'Sin subcarpetas.', select: 'Seleccionar esta carpeta' },
  session: {
    title: 'Nueva sesión',
    model: 'Modelo', modelDesc: 'Predeterminado usa tu configuración de Claude; elige un modelo específico si prefieres.',
    modelDefault: 'Predeterminado', modelFable: 'Fable (más capaz)', modelOpus: 'Opus',
    modelSonnet: 'Sonnet', modelHaiku: 'Haiku (más rápido)',
    continueTitle: 'Continuar conversación', continueDesc: 'Retoma la última conversación de esta carpeta, si existe.',
    skipTitle: 'Omitir permisos', skipDesc: 'Claude ejecuta comandos sin pedir tu confirmación en cada paso.',
    permWarning: 'Con esto desactivado, las acciones que requieren permiso se deniegan automáticamente en el chat web. Para aprobar interactivamente, usa “Abrir en la terminal”.',
    start: 'Iniciar sesión',
  },
  chat: {
    select: 'Selecciona una sesión.', openInTerminal: 'Abrir en la terminal',
    inTerminalNotice: 'Esta sesión está abierta en la terminal. Ciérrala allí para retomar en la web.',
    placeholder: 'Mensaje para Claude Code… (arrastra o pega archivos)',
    placeholderWorking: 'sesión trabajando…', placeholderUploading: 'subiendo adjunto…',
    turnDone: 'turno terminado', turnError: 'el turno terminó con error',
    thinking: 'Pensamiento', forward: 'Reenviar', forwardedTo: 'reenviado a {{name}}',
    noAgents: 'Ningún otro agente activo', subagent: 'subagente',
  },
  toolcall: { result: 'Resultado:' },
  terminal: {
    close: 'Cerrar terminal', reconnecting: 'desconectado — reconectando…',
    disconnected: 'desconectado', reconnect: 'Reconectar',
    openError: '[error al abrir la terminal: {{error}}]',
  },
  mural: { title: 'Tablón', empty: 'El tablón está vacío. Los agentes publican avisos aquí.' },
  tasks: {
    title: 'Tareas',
    empty: 'Aún no se han despachado tareas. Un agente puede delegar con la herramienta despachar_tarefa.',
    inProgress: 'en curso', done: 'completada', failed: 'falló', operator: 'operador',
  },
  notify: { needsAttention: 'terminó y te espera', died: 'la sesión murió' },
}
export default es
```

- [ ] **Step 5: `web/src/i18n/index.ts` + `i18next.d.ts`**

`index.ts`:
```ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import en from './en'
import es from './es'
import ptBR from './pt-BR'

const STORAGE_KEY = 'claudinei.locale'
export const LOCALES = [
  { code: 'en', flag: '🇺🇸', name: 'English' },
  { code: 'es', flag: '🇪🇸', name: 'Español' },
  { code: 'pt-BR', flag: '🇧🇷', name: 'Português' },
] as const

function savedLocale(): string {
  try {
    const v = localStorage.getItem(STORAGE_KEY)
    return v && LOCALES.some((l) => l.code === v) ? v : 'en'
  } catch { return 'en' }
}

void i18n.use(initReactI18next).init({
  resources: { en: { translation: en }, es: { translation: es }, 'pt-BR': { translation: ptBR } },
  lng: savedLocale(),
  fallbackLng: 'en',
  interpolation: { escapeValue: false }, // React já escapa
})

i18n.on('languageChanged', (lng) => {
  try { localStorage.setItem(STORAGE_KEY, lng) } catch { /* storage indisponível: só não persiste */ }
  document.documentElement.lang = lng
})
document.documentElement.lang = i18n.language

export default i18n
```

`i18next.d.ts`:
```ts
import type en from './en'

declare module 'i18next' {
  interface CustomTypeOptions {
    resources: { translation: typeof en }
  }
}
```

Em `web/src/main.tsx`: adicionar `import './i18n'` ANTES do import do App.
Em `web/src/test/setup.ts`: adicionar ao final:
```ts
import i18n from '../i18n'
void i18n.changeLanguage('pt-BR') // asserções existentes são pt-BR
```

- [ ] **Step 6: Teste de infra (falhando) — `web/src/test/i18n.test.tsx`**

```tsx
import { describe, it, expect, afterEach } from 'vitest'
import i18n from '../i18n'

afterEach(() => { void i18n.changeLanguage('pt-BR') }) // devolve o default dos testes

describe('i18n', () => {
  it('resolve a mesma chave nas 3 línguas', async () => {
    await i18n.changeLanguage('en')
    expect(i18n.t('sidebar.terminals')).toBe('Terminals')
    await i18n.changeLanguage('es')
    expect(i18n.t('sidebar.terminals')).toBe('Terminales')
    await i18n.changeLanguage('pt-BR')
    expect(i18n.t('sidebar.terminals')).toBe('Terminais')
  })

  it('interpola variáveis', async () => {
    await i18n.changeLanguage('pt-BR')
    expect(i18n.t('confirm.deleteTitle', { name: 'Alpha' })).toBe('Excluir Alpha?')
  })

  it('persiste a troca no localStorage', async () => {
    await i18n.changeLanguage('es')
    expect(localStorage.getItem('claudinei.locale')).toBe('es')
    expect(document.documentElement.lang).toBe('es')
  })
})
```

- [ ] **Step 7: `LanguageSwitcher.tsx` + CSS + Sidebar**

`web/src/components/LanguageSwitcher.tsx`:
```tsx
import { useEffect, useRef, useState } from 'react'
import { createPortal } from 'react-dom'
import { useTranslation } from 'react-i18next'
import { LOCALES } from '../i18n'

export function LanguageSwitcher() {
  const { i18n, t } = useTranslation()
  const [open, setOpen] = useState(false)
  const [pos, setPos] = useState<{ top: number; left: number }>({ top: 0, left: 0 })
  const btnRef = useRef<HTMLButtonElement>(null)
  const current = LOCALES.find((l) => l.code === i18n.language) ?? LOCALES[0]

  useEffect(() => {
    if (!open) return
    const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') setOpen(false) }
    window.addEventListener('keydown', onKey)
    return () => window.removeEventListener('keydown', onKey)
  }, [open])

  const toggle = () => {
    const r = btnRef.current?.getBoundingClientRect()
    if (r) setPos({ top: r.bottom + 6, left: r.left })
    setOpen((o) => !o)
  }

  return (
    <>
      <button ref={btnRef} className="lang-switcher" title={t('lang.label')} aria-label={t('lang.label')} onClick={toggle}>
        {current.flag}
      </button>
      {open && createPortal(
        <div className="lang-menu__overlay" onClick={() => setOpen(false)}>
          <div className="lang-menu glass" style={{ top: pos.top, left: pos.left }} onClick={(e) => e.stopPropagation()}>
            {LOCALES.map((l) => (
              <div key={l.code}
                   className={`lang-menu__item ${l.code === current.code ? 'active' : ''}`}
                   onClick={() => { void i18n.changeLanguage(l.code); setOpen(false) }}>
                <span className="lang-menu__flag">{l.flag}</span>
                <span className="lang-menu__name">{l.name}</span>
                {l.code === current.code && <span className="lang-menu__check">✓</span>}
              </div>
            ))}
          </div>
        </div>,
        document.body,
      )}
    </>
  )
}
```

CSS (fim de `web/src/styles.css`):
```css
/* Seletor de idioma */
.lang-switcher {
  background: transparent; border: 1px solid var(--glass-border); border-radius: 999px;
  font-size: 15px; line-height: 1; padding: 4px 9px; cursor: pointer;
  transition: background .15s ease, border-color .15s ease;
}
.lang-switcher:hover { background: var(--glass-bg-strong); border-color: var(--accent); }
.lang-menu__overlay { position: fixed; inset: 0; z-index: 60; }
.lang-menu { position: fixed; min-width: 170px; border-radius: 12px; padding: 6px; z-index: 61; }
.lang-menu__item {
  display: flex; align-items: center; gap: 9px; padding: 8px 10px; border-radius: 8px;
  cursor: pointer; font-size: 14px;
}
.lang-menu__item:hover { background: var(--glass-bg-strong); }
.lang-menu__item.active { background: var(--glass-bg-strong); }
.lang-menu__flag { font-size: 16px; }
.lang-menu__name { flex: 1; }
.lang-menu__check { color: var(--accent); font-weight: 600; }
```

Na `Sidebar.tsx`:
- `import { useTranslation } from 'react-i18next'` e `import { LanguageSwitcher } from './LanguageSwitcher'`; no componente: `const { t } = useTranslation()`.
- A linha do logo vira flex com o switcher à direita:
```tsx
      <div className="sidebar__top">
        <div className="sidebar__logo" onClick={openDashboard} title={t('sidebar.overview')}>
          <span className="sidebar__logo-star">✳</span> Claudinei
        </div>
        <LanguageSwitcher />
      </div>
```
CSS extra: `.sidebar__top { display: flex; align-items: center; justify-content: space-between; margin: 2px 4px 14px; }` e remover o margin da `.sidebar__logo` (vira `margin: 0`).
- Migrar TODAS as strings da Sidebar: `t('sidebar.terminals')`, `t('sidebar.addTerminal')`, `t('sidebar.noSession')`, `t('sidebar.empty')`, `t('sidebar.interaction')`, `t('sidebar.mural')`, `t('sidebar.tasks')`, titles `t('sidebar.revive')`/`t('sidebar.startSession')`/`t('sidebar.editTerminal')`/`t('sidebar.deleteTerminal')`, status `t(('status.' + s.status) as any)` → tipar com união: usar `t(\`status.${s.status}\` as const)` se o TS reclamar, senão manter o acesso via template literal types (react-i18next v15 suporta). ConfirmDialog da Sidebar: `title={t('confirm.deleteTitle', { name: deleteFor.name })}` e `message={t('confirm.deleteMsg')}` e `confirmLabel={t('common.delete')}`.
- Remover o import de `STATUS_LABEL` da Sidebar.
- `web/src/test/sidebar.test.tsx`: trocar `STATUS_LABEL.working` por literal `'trabalhando'` (o setup fixa pt-BR) e remover o import.

- [ ] **Step 8: Teste do switcher (adicionar ao `i18n.test.tsx`)**

```tsx
import { render, screen, fireEvent, cleanup } from '@testing-library/react'
import { LanguageSwitcher } from '../components/LanguageSwitcher'
import { Sidebar } from '../components/Sidebar'
import { useStore } from '../store'

describe('LanguageSwitcher', () => {
  afterEach(() => cleanup())

  it('abre o menu, troca para inglês e a sidebar re-renderiza', async () => {
    useStore.setState({ projects: [], sessions: {}, chat: {}, unread: {}, streaming: {}, historyLoadedFor: {}, view: 'dashboard', activeLocalId: undefined })
    render(<Sidebar />)
    expect(screen.getByText('Terminais')).toBeTruthy()
    fireEvent.click(screen.getByLabelText('Idioma'))
    fireEvent.click(screen.getByText('English'))
    expect(await screen.findByText('Terminals')).toBeTruthy()
  })

  it('marca o idioma ativo com ✓', async () => {
    await i18n.changeLanguage('es')
    render(<LanguageSwitcher />)
    fireEvent.click(screen.getByLabelText('Idioma'))
    const active = screen.getByText('Español').closest('.lang-menu__item')
    expect(active?.querySelector('.lang-menu__check')).toBeTruthy()
  })
})
```

- [ ] **Step 9: Rodar, verificar, commitar**

Run: `npm test -w web && npm run build -w web`
Expected: suíte inteira verde (incl. i18n.test) e build limpo.

```bash
git add web/src/i18n web/src/components/LanguageSwitcher.tsx web/src/components/Sidebar.tsx web/src/main.tsx web/src/test/setup.ts web/src/test/i18n.test.tsx web/src/test/sidebar.test.tsx web/src/styles.css web/package.json package-lock.json
git commit -m "feat(web): infra i18n (react-i18next, en/es/pt-BR tipados) + seletor com bandeiras + Sidebar migrada"
```

---

### Task 2: Migrar Dashboard, ProjectCard e modais

**Files:**
- Modify: `web/src/components/Dashboard.tsx`, `ProjectCard.tsx`, `NewProjectModal.tsx`, `StartSessionModal.tsx`, `FolderPicker.tsx`, `ProjectPreviewCard.tsx`
- Test: os testes existentes desses componentes seguem verdes (setup pt-BR); nenhum novo teste é necessário além de manter os atuais.

**Interfaces:**
- Consumes: chaves já definidas na Task 1 (`dashboard.*`, `projectCard.*`, `modal.*`, `folder.*`, `session.*`, `common.*`, `status.*`, `confirm.*`).

- [ ] **Step 1: Migração mecânica**

Em cada componente: `const { t } = useTranslation()` (import de 'react-i18next') e trocar cada literal pela chave, conforme a tabela:

| Componente | Literal atual (pt) | Chave |
|---|---|---|
| Dashboard | `Projetos` | `dashboard.projects` |
| Dashboard | `Nenhum projeto ainda. Crie o primeiro!` | `dashboard.empty` |
| ProjectCard | title `Excluir projeto` | `projectCard.deleteTitle` |
| ProjectCard | `Reviver` | `projectCard.revive` |
| ProjectCard | `Iniciar sessão` | `projectCard.startSession` |
| ProjectCard | `STATUS_LABEL[session.status]` | `t('status.' + session.status)` |
| ProjectCard | ConfirmDialog title/message/label | `confirm.deleteTitle`(name)/`confirm.deleteMsg`/`common.delete` |
| NewProjectModal | `Editar terminal`/`Novo projeto` | `modal.editTerminal`/`modal.newProject` |
| NewProjectModal | placeholder `Nome do projeto` | `modal.namePlaceholder` |
| NewProjectModal | `Escolher pasta…` | `modal.choosePath` |
| NewProjectModal | `Prévia` | `common.preview` |
| NewProjectModal | `Cancelar`/`Salvar`/`Criar` | `common.cancel`/`common.save`/`common.create` |
| ProjectPreviewCard | `pré-visualização` | `modal.previewTag` |
| StartSessionModal | `Nova sessão` | `session.title` |
| StartSessionModal | `Modelo` + desc | `session.model`/`session.modelDesc` |
| StartSessionModal | options Padrão/Fable/Opus/Sonnet/Haiku | `session.modelDefault`/`session.modelFable`/`session.modelOpus`/`session.modelSonnet`/`session.modelHaiku` |
| StartSessionModal | `Continuar conversa` + desc | `session.continueTitle`/`session.continueDesc` |
| StartSessionModal | `Pular permissões` + desc | `session.skipTitle`/`session.skipDesc` |
| StartSessionModal | aviso ⚠️ (texto do warning) | `session.permWarning` |
| StartSessionModal | `Cancelar`/`Iniciar sessão` | `common.cancel`/`session.start` |
| FolderPicker | `Escolher pasta` | `folder.title` |
| FolderPicker | `Nenhuma subpasta.` | `folder.empty` |
| FolderPicker | `Selecionar esta pasta` | `folder.select` |
| FolderPicker | `Cancelar` | `common.cancel` |

Regra: literais não listados que sejam visíveis ao usuário → criar chave na convenção da área nas 3 línguas e anotar no report. `aria-label`s de controles puramente técnicos (ex.: "cor") podem usar as mesmas chaves quando existirem.

- [ ] **Step 2: Rodar e commitar**

Run: `npm test -w web && npm run build -w web`
Expected: suíte inteira verde (asserções pt continuam válidas — setup pt-BR); build limpo.

```bash
git add web/src/components/Dashboard.tsx web/src/components/ProjectCard.tsx web/src/components/NewProjectModal.tsx web/src/components/StartSessionModal.tsx web/src/components/FolderPicker.tsx web/src/components/ProjectPreviewCard.tsx
git commit -m "feat(web): i18n em Dashboard, ProjectCard e modais"
```

---

### Task 3: Migrar chat/terminal/painéis/notificações + remover STATUS_LABEL

**Files:**
- Modify: `web/src/components/ChatView.tsx`, `ChatInput.tsx`, `MessageBlock.tsx`, `ToolCallCard.tsx`, `TerminalView.tsx`, `MuralPanel.tsx`, `TasksPanel.tsx`, `web/src/notifications.ts`, `web/src/types.ts` (remove STATUS_LABEL)
- Test: atualizar apenas testes que importam STATUS_LABEL (trocar por literal pt); suíte inteira verde.

**Interfaces:**
- Consumes: chaves da Task 1 (`chat.*`, `toolcall.*`, `terminal.*`, `mural.*`, `tasks.*`, `notify.*`, `status.*`).

- [ ] **Step 1: Migração mecânica**

| Componente | Literal | Chave |
|---|---|---|
| ChatView | `Selecione uma sessão.` | `chat.select` |
| ChatView | `Abrir no terminal` | `chat.openInTerminal` |
| ChatView | aviso in_terminal | `chat.inTerminalNotice` |
| ChatView | `STATUS_LABEL[session.status]` | `t('status.' + session.status)` |
| ChatInput | placeholders (3) | `chat.placeholderWorking`/`chat.placeholderUploading`/`chat.placeholder` |
| ChatInput | `Enviar` | `common.send` |
| MessageBlock | `Pensamento` | `chat.thinking` |
| MessageBlock | `Encaminhar` | `chat.forward` |
| MessageBlock | `encaminhado para {nome}` | `t('chat.forwardedTo', { name })` |
| MessageBlock | `Nenhum outro agente ativo` | `chat.noAgents` |
| MessageBlock | `↳ subagente` | `↳ {t('chat.subagent')}` |
| MessageBlock | `turno concluído`/`turno terminou com erro` | `chat.turnDone`/`chat.turnError` |
| ToolCallCard | `Resultado:` | `toolcall.result` |
| TerminalView | `Encerrar terminal` | `terminal.close` |
| TerminalView | banner reconectando/desconectado/Reconectar | `terminal.reconnecting`/`terminal.disconnected`/`terminal.reconnect` |
| TerminalView | `[erro ao abrir terminal: ...]` | `t('terminal.openError', { error: String(err) })` |
| MuralPanel | `Mural` (h2, emoji fica fora) | `mural.title` |
| MuralPanel | empty state | `mural.empty` |
| TasksPanel | `Tarefas` (h2) | `tasks.title` |
| TasksPanel | empty state | `tasks.empty` |
| TasksPanel | STATUS map em_andamento/concluída/falhou | `tasks.inProgress`/`tasks.done`/`tasks.failed` |
| TasksPanel | `operador` | `tasks.operator` |
| notifications.ts | `terminou e aguarda você` | `i18n.t('notify.needsAttention')` (import de `./i18n`) |
| notifications.ts | `sessão morreu` | `i18n.t('notify.died')` |

- Remover `STATUS_LABEL` de `web/src/types.ts` (último consumidor migrado). Atualizar qualquer teste que o importe (grep) para literais pt.
- notifications.ts é fora do React: `import i18n from './i18n'` e usar `i18n.t(...)` no momento da notificação (não em constantes de módulo — o idioma pode mudar).
- TasksPanel: o map de status vira função que usa `t()` dentro do componente.

- [ ] **Step 2: Varredura de completude**

Run: `grep -rnE "'[^']*[áéíóúçãõê][^']*'|\"[^\"]*[áéíóúçãõê][^\"]*\"" web/src/components web/src/notifications.ts web/src/types.ts | grep -v test`
Expected: nenhum literal pt visível restante (comentários são ok). O que sobrar visível → migrar.

- [ ] **Step 3: Rodar e commitar**

Run: `npm test -w web && npm run build -w web`
Expected: suíte inteira verde; build limpo.

```bash
git add web/src/components web/src/notifications.ts web/src/types.ts web/src/test
git commit -m "feat(web): i18n em chat/terminal/painéis/notificações; remove STATUS_LABEL"
```

---

## Self-Review

**1. Spec coverage:** lib B (i18next+react-i18next, sem detector) → Task 1 ✅; default en + persistência claudinei.locale + html lang + fallback → Task 1 index.ts ✅; chaves tipadas (typeof en + d.ts) → Task 1 ✅; seletor com bandeiras/Portal/Esc/✓ → Task 1 ✅; setup de teste pt-BR → Task 1 ✅; migração completa da UI → Tasks 1-3 (tabelas) ✅; notificações fora do React → Task 3 ✅; STATUS_LABEL removido no fim → Task 3 ✅; backend fora do escopo ✅.
**2. Placeholder scan:** dicionários COMPLETOS nas 3 línguas no plano; tabelas de mapeamento por componente; nenhuma instrução vaga além da regra explícita de "literal não listado → criar chave e anotar". ✅
**3. Type consistency:** `LOCALES` exportado por i18n/index e consumido no switcher; chaves das tabelas existem todas no `en.ts` do Step 2; `t('status.' + s)` casa com `status.*` cobrindo os 7 estados de SessionStatus. ✅
